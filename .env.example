# PesaNet WiFi Billing System — Environment Variables
# Copy this to .env and fill in your values

# Database — SQLite file path (use volume-mounted path in Docker)
DATABASE_URL="file:/data/pesanet.db"

# Next.js
NODE_ENV=production

# Server
PORT=3000
HOSTNAME=0.0.0.0
